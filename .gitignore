# build trees (dev builds run inside the KDE Flatpak SDK)
build/
build-*/

# Qt Creator / editor cruft
*.user
*.kate-swp
.kdev4/
.idea/
.vscode/

# core dumps / logs (the bare `core` pattern would also match the
# src/core/ package, so it is explicitly re-included below)
core
!src/core/
*.log
